experiment_configs = [
    {"model_name": "logreg", "use_cluster_features": True, "apply_smote": True, "use_gpu": True},
    {"model_name": "svm", "use_cluster_features": True, "apply_smote": True, "use_gpu": True},
    {"model_name": "naive_bayes", "use_cluster_features": True, "apply_smote": True, "use_gpu": False},
    {"model_name": "neural_net", "use_cluster_features": True, "apply_smote": True, "use_gpu": True},
    {"model_name": "decision_tree", "use_cluster_features": True, "apply_smote": True, "use_gpu": True},
    {"model_name": "rf", "use_cluster_features": True, "apply_smote": True, "use_gpu": True},
    {"model_name": "xgboost", "use_cluster_features": True, "apply_smote": True, "use_gpu": True}
]
